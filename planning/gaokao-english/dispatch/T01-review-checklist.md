# T01 独立评审清单

状态：仅完成验收准备，全部产品检查待交付后执行；未作 PASS 判断，T02 保持 HOLD。

依据：`T01.md`、规划 README、实施计划 T01、D01、P05。阶段冲突以 T01 合同的明确收敛为准，不扩展为 T02 或完整产品验收。采用编排技能的独立证据审查原则；不启用外部协作者或调用其他模型。

## A. 交付基线与保护范围

- [ ] 主代理确认 GLM 本轮写入/自测结束，固定交付文件清单及摘要；验证实际 cwd、分支、完整 HEAD 与交接基线 `7478ac2` 对应，不在并发修改期间验收。
- [ ] 取得初始及最终 tracked/untracked/ignored 文件清单；同时核对暂存区及删除项。默认 `git diff` 不足以覆盖整个新增应用。
- [ ] 完整补丁包含全部新增源文件、lockfile、测试、类型声明及配置。必要的 T01 额外配置/冒烟文件逐项有用途说明，排除 node_modules、缓存等非交付依赖目录。
- [ ] 若采用直连 API 补丁方式，核对 GLM-5.3 模型标识、可见原始补丁/摘要与实际应用差异，确认无截断或主代理代写修复；安装生成文件单独说明。HTTP 200、reasoning-only 响应不能作为代码交付，TDD 红绿证据仍须按顺序真实产生。
- [ ] 对照开工时 planning 清单/摘要保护用户材料；不能把本轮主目录协调文档更新误记为 GLM 修改，也不能仅用提交基线验证未跟踪 planning。缺初始证据则明确保留未验证项。
- [ ] 应用外原站点、根 package/lockfile、用户材料未被修改；根 `.gitignore` 只允许单独建议补丁。无全局配置变更、暂存、提交、推送或部署。

## B. 工程、依赖和范围审查

- [ ] 独立应用 `apps/english-practice`；包名 `gaokao-english-practice`，版本 `0.1.0`，`private: true`、ESM、Node engines `>=24 <25`；根目录未改为 workspace。
- [ ] Node/npm 实际路径和版本均已记录，当前进程 PATH 指向指定 Node 24；不凭绝对 node 路径推定 npm 实际使用同一运行时。
- [ ] 运行依赖包含 Vue 3、Vue Router 4、Dexie 4、Zod 4；开发依赖逐项对照实施计划 T01 安装清单（含 tsx、@types/node、PWA/测试/lint 工具），全部精确版本并与独立 lockfile 一致。
- [ ] 审查 registry 版本、engines、peer dependencies 兼容证据与安装日志，未用 `--force`、`--legacy-peer-deps` 等绕过冲突；必要时独立查询补证，不把版本存在当作兼容证明。
- [ ] TypeScript strict 真正覆盖应用；Vue+TypeScript ESLint flat config 有效，Vitest 使用 jsdom。审查 include/exclude/ignore、规则豁免、跳过测试及异常处理，不用空配置或 `passWithNoTests` 假通过。
- [ ] scripts 对照原计划：dev `0.0.0.0:5174`、preview `127.0.0.1:4174` 且 strictPort；lint 在 T01 可为 `eslint src tests`；其余入口及后续内容命令保留契约。
- [ ] content 三条命令明确 T02 待实现，没有永远成功的占位实现；`test:e2e` 零测试不得计为通过；完整 `npm run build/check` 不列入本阶段通过结论。
- [ ] 无 T02 内容协议/打包、数据库、学习会话、成绩、判题、下载、备份、PWA 注册/缓存、后端或 AI 功能；无运行时第三方 CDN 字体/脚本。安装依赖不算实现对应功能。

## C. 行为与测试真实性

- [ ] `createWebHashHistory()` 实际生效，根入口导向 `/#/today`，不是仅静态呈现页面。
- [ ] 首页按钮有真实 button 角色及可访问名称“开始今日练习”；点击显示明确无课程提示，重复操作稳定，无虚假课程、会话、成功成绩或无效下载入口。
- [ ] 交付红灯证明目标行为缺失；依赖缺失/测试配置错误不算行为红灯。核对同一用例的后续绿灯，不事后编造 TDD 记录。
- [ ] 单元测试覆盖按钮角色/名称、点击/重复点击、真实路由初始化及异步更新；正确等待更新，未吞未处理异常、未用过度 mock 绕过目标行为。

## D. 独立命令复跑

仅在主代理确认交付稳定后执行。在应用目录以当前进程 PATH 使用 Node 24，先审查 package scripts 与安装钩子，再逐条运行。`npx vite build` 必须使用已安装的本地 Vite，不临时下载替代品。

| 命令 | 预期 | 本轮状态 |
| --- | --- | --- |
| `npm ci` | exit 0，按 lockfile 可复现安装，清单/lockfile 不漂移 | 待执行 |
| `npm run typecheck` | exit 0，真实类型检查 | 待执行 |
| `npm run lint` | exit 0，真实 Vue/TS 检查 | 待执行 |
| `npm run test:unit` | exit 0，实际测试数大于 0、无未处理异常 | 待执行 |
| `npx vite build` | exit 0，实际生产产物生成 | 待执行 |

- [ ] 每条记录 cwd、命令、环境、退出码、测试数量/关键输出和脱敏证据路径；明确独立复跑证据与 GLM 自测证据的区别。
- [ ] 复跑前后核对交付源文件摘要；修正后旧 PASS 不自动沿用，复跑受影响门禁。

## E. 生产预览与浏览器证据

主代理负责后续浏览器执行，本协调任务核对证据覆盖及对应产物，必要复测由主代理协调，避免端口/进程冲突。截图不能代替交互、控制台及尺寸检查记录。

- [ ] preview 确认服务归属、实际 URL `http://127.0.0.1:4174/#/today`、生产构建及浏览器版本。端口占用回报，不杀其他进程、不暗换端口。
- [ ] 根入口跳转、直接访问 `/#/today`、刷新均正常；按钮点击/触控、重复点击及明确空态有效，无控制台异常或未处理页面错误。
- [ ] 360x640、390x844、768x1024、1024x768 分别记录截图及溢出/遮挡检查；手机单列、平板布局合理，无需为 T01 空态提前实现阅读双栏。
- [ ] 所有可操作目标实测至少 44x44 CSS px；正文计算字号默认 18px；无悬停或拖动依赖，Tab 可达并可用 Enter/Space 激活。
- [ ] 200% 缩放后文字和操作仍可达，不禁止缩放；记录具体缩放方法，不能把 deviceScaleFactor=2 或单纯高像素截图当作 200% 页面缩放证据。
- [ ] 浏览器日志、截图及自动化证据对应已验收交付摘要；未做手机/平板真机测试明确写“未验证”，不升级为 Safari/iOS/PWA/离线验收。
- [ ] 完成后仅由服务所有者关闭本任务自行启动的服务；记录浏览器环境缺失等准确阻塞，不能以构建成功替代。

## F. 结论与返工格式

- [ ] 收齐基线、完整补丁、新增文件、依赖兼容证据、红绿测试、独立五条命令结果、浏览器证据和未验证边界。
- [ ] 每个问题记录严重性、文件/行号、合同条款、复现命令/行为、实际与预期、证据及要求 GLM 修正的最小范围；不由协调任务代改产品代码。
- [ ] 结论区分：证据未交付为 PENDING；行为/配置违约为 FAIL；外部条件确实阻断检查为 BLOCKED；全部 T01 必需门禁有证据才为 PASS。
- [ ] 在 dispatch 保存验收记录草稿：任务 T01、基线完整 SHA、提交 SHA“未提交”、文件摘要、独立审查/命令/浏览器结果及边界；正式 `verification-log.md` 由主代理维护。
- [ ] 只有 Astra 明确 T01 PASS 后才可由主代理派发 T02；评审准备完成、GLM 自测成功或预览页面出现都不等于放行。
